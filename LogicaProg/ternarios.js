// ? :

// substituindo o if e else
// (CONDIÇÃO) ? 'Valor se verdadeiro' : 'Valor se falso'

const idade = 18;
const maiorIdade = idade >= 18 ? 'Usuário maior de idade' : 'Usuário menor de idade';

console.log(maiorIdade);
